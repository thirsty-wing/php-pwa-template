# CSS Strategy

The strategy for styling the application is to write CSS using [Reasonable System for CSS Stylesheet Structure (RSCSS)](https://rstacruz.github.io/rscss/). Using inline styles is also ok, but we don't mix the two on the same element.
